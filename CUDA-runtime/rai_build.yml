rai:
  version: 0.2
resources:
  cpu:
    architecture: ppc64le
  network: false
commands:
  build_image:
    image_name: c3sr/cuda-runtime_ppc64le:8.0
    dockerfile: "./Dockerfile"
    # no_cache: true
    push:
      push: true