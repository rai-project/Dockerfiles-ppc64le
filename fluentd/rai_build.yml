rai:
  version: 0.2
resources:
  cpu:
    architecture: ppc64le
  network: false
commands:
  build_image:
    image_name: c3sr/fluentd_ppc64le:0.12.34
    dockerfile: "./Dockerfile"
    # no_cache: true
    push:
      push: true